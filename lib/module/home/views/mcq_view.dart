import 'package:flutter/material.dart';

class McqView extends StatelessWidget {
  const McqView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("MCQ Section", style: TextStyle(fontSize: 18)),
    );
  }
}
